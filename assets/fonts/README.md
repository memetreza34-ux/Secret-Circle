# Schriften

Beide Familien stehen unter der SIL Open Font License 1.1 (`OFL.txt`).

| Datei | Familie | Quelle |
| --- | --- | --- |
| `anton-latin.woff2` | Anton | Google Fonts, Subset `latin` |
| `figtree-latin.woff2` | Figtree (variabel) | Google Fonts, Subset `latin` |

Sie liegen lokal im Repo, damit die App offline identisch aussieht. Ein CDN-Verweis würde die
Offline-Fähigkeit brechen.

Die Copyright-Zeilen der einzelnen Familien stehen in den jeweiligen Google-Fonts-Repositories
(`ofl/anton`, `ofl/figtree`) und gehören vor einer Store-Veröffentlichung in
den Lizenzhinweis der App.
