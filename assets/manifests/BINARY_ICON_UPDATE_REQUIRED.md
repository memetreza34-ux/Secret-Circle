# Binary icon update marker

The replacement `icon.svg` is now committed. The existing root PNG files still need to be replaced with the prepared 192×192 and 512×512 derivatives before the asset gate can be reclassified. This marker prevents the temporary mixed state from being mistaken for completed asset sign-off.
