{
  "name": "secret-circle",
  "version": "1.0.0-beta.3",
  "private": true,
  "description": "Offline-first Imposter-Partyspiel als installierbare PWA.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node tests/engine.test.js && node tests/storage.test.js && node tests/content.test.js && node tests/fuzz.test.js",
    "check": "node --check runtime-guard.js && node --check setup-ux.js && node --check privacy-guard.js && node --check wake-lock.js && node --check app.js && node --check game-engine.js && node --check data-store.js && node --check word-packs.js && node --check sw.js",
    "validate": "python scripts/repo_hygiene.py && python scripts/validate_project.py && python scripts/performance_budget.py && python scripts/release_audit.py",
    "test:e2e": "playwright test",
    "test:cross-browser": "playwright test --config=playwright.cross-browser.config.js",
    "ci": "npm run check && npm test && npm run validate && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.54.2"
  }
}
